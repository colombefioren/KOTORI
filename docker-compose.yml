services:
  kotori:
    build: .
    image: kotori
    ports:
      - "7860:7860"
    env_file:
      - .env
    environment:
      KOTORI_DATA_DIR: /data
    volumes:
      - ./data:/data
    restart: unless-stopped
